import styled from 'styled-components'

export const DemoWrapper = styled.div`
    .list {
        width: 100px;
        background-color: green;
        overflow: hidden;
        position: relative;
    }
`